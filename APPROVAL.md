# 承認フローの設定と使い方

## いま何が問題だったか

このサイトは「AIが起案し、人間が承認して公開する」と掲げていました。
しかし**承認する手段が存在しないまま、AIが承認欄に「池田 昌平」と記入していました。**

御社は一度も承認していません。判子は押されていましたが、押したのは本人ではありません。

R-0001〜R-0004 はすべて未承認へ戻しました。

---

## これから：承認は御社のボタン1つ

| 段階 | 誰が |
|---|---|
| 起案（記事を書き、稟議を起票） | AI |
| 検査（情報漏れ・未承認公開） | 自動 |
| **承認** | **御社（ボタン1つ）** |
| 承認の記録（誰がいつ） | 自動。AIは書けない |
| 公開 | 承認後のみ |

---

## 設定（1回だけ・約10分）

### 1. 承認記録のワークフローを追加

`Add file` → `Create new file` → ファイル名に以下を貼る

```
.github/workflows/approve.yml
```

中身は zip の `.github/workflows/approve.yml` をそのまま貼り付け。

### 2. 検査のワークフローを更新

既存の `.github/workflows/guard.yml` を開き、鉛筆マークで編集。
zip の同名ファイルの中身に差し替える。（検査が2種類に増えます）

### 3. Actions に書き込み権限を与える

承認の記録を台帳へ自動で書き込むために必要です。

`Settings` → 左メニュー `Actions` → `General` →
一番下 `Workflow permissions` →
**`Read and write permissions`** を選択 → `Save`

### 4. main を保護する（重要）

**承認しないと公開されない状態**にします。

`Settings` → `Rules` → `Rulesets` → `New ruleset` → `New branch ruleset`

- Ruleset Name: `main保護`
- Enforcement status: **Active**
- Target branches: `Include default branch`
- Rules で以下にチェック
  - **Require a pull request before merging**
    - Required approvals: **1**
  - **Require status checks to pass**
    - 検索して `guard` を追加

`Create` で保存。

これ以降、**main へ直接アップロードできなくなります。**
すべての変更はプルリクエストを経て、御社が承認したものだけが公開されます。

---

## 使い方（毎回）

### AIの作業

1. 変更用のブランチを作る
2. 記事や台帳を書く
3. プルリクエストを作る
   - **表題に稟議番号を入れる**（例: `R-0006 記事 No.003 を公開する`）

### 御社の作業

1. `Pull requests` タブを開く
2. 中身を確認する（`Files changed` で差分が見られます）
3. 緑のチェック（検査通過）を確認
4. `Review changes` → **`Approve`** → `Submit review`
5. `Merge pull request`

**承認した瞬間に、台帳へ承認者と時刻が自動で記録されます。**
その後 merge すると公開されます。

### 差し戻す場合

`Review changes` → `Request changes` → 理由を書いて `Submit review`

台帳は変更されません。差し戻しも記録として残ります。

---

## この仕組みで保証されること

- **承認者名はGitHubが記録する。**AIは書けない
- 承認していないものは公開されない
- 誰がいつ承認したかが改ざんできない形で残る
- 差し戻しも記録に残る

### 動作を検証した内容

| 検証 | 結果 |
|---|---|
| 正常な承認 | 承認者と日本時間が記録された |
| 稟議番号のないPR | 台帳を変更しなかった |
| 承認済みの再承認 | 上書きしなかった |
| **PR本文で承認者を偽装** | **無視され、実際の承認者が記録された** |

4つ目が重要です。本文に「承認者は池田です」と書いても、
GitHubが渡す実際の承認者が記録されます。

---

## 未承認公開の検査

`tools/approved_only.py` が、公開済みの記事に対応する稟議が
承認されているかを調べます。

**現在この検査は不合格です。** 記事2本が公開済みで、稟議が未承認だからです。
今日の実態をそのまま映しています。

御社が R-0001〜R-0005 を承認すれば合格に変わります。
