# 自動化のセットアップ

作業時間：15分ほど

## 何ができるようになるか

```
毎朝5時（自動）
  ↓ 公式RSSから収集
  ↓ 記事を起案（Claude API）
  ↓ 検査（情報漏れ／未承認公開）
  ↓ 検査を通ったものだけプルリクエストにする
  ↓
朝、承認ボタンを押す ← 御社の作業はここだけ
  ↓
公開
```

**検査で止まれば、プルリクエストは作られません。**

---

## 1. ファイルをアップロード

`Add file` → `Upload files` で、フォルダ構造ごとドロップ。

### 新規
- `feeds.json` — 取得先の設定
- `tools/collect.py` — 公開情報の収集
- `tools/write.py` — 記事の起案
- `tools/publish.py` — 記事ページと台帳の作成
- `tools/pr_body.py` — 申請文面の作成
- `tools/count_drafts.py` — 起案本数の判定

### 更新
- `tools/approve.py` — 承認時に記事も公開済みへ

### 画面から作る（隠しフォルダのため）
- `.github/workflows/daily.yml`

`Add file` → `Create new file` → ファイル名に
`.github/workflows/daily.yml` を貼り、中身を貼り付け。

**mainが保護されているので、プルリクエスト経由になります。**
承認してマージしてください。

---

## 2. Actions に書き込み権限を与える

`Settings` → `Actions` → `General` → 一番下 `Workflow permissions`
→ **`Read and write permissions`** → `Save`

さらに、その下の
**`Allow GitHub Actions to create and approve pull requests`**
にチェックを入れてください。**これがないとPRを作れません。**

---

## 3. 手動で動かして確認する

`Actions` タブ → 左メニュー **`毎朝の起案`** → 右の **`Run workflow`**

1〜2分で終わります。

### 想定される結果

| 結果 | 意味 |
|---|---|
| PRが1件できる | 正常。承認してください |
| 「起案する材料がありませんでした」 | 正常。収集0件かつ日報が少ない日 |
| 赤いバツ | どこかで止まっています |

---

## 承認の仕方

1. `Pull requests` タブを開く
2. 中身を確認（`Files changed`）
3. `Review changes` → **`Approve`** → `Submit review`
4. `Merge pull request`

**AIが起案者なので、御社が承認できます。**
（自分で作ったPRは自分で承認できない仕様のため、自動化が必要でした）

---

## 費用について

記事1本あたり 0.05〜0.15ドル程度の見込みです。
2本立てを毎日回すと、**現在の残高では2〜3週間**で尽きます。

尽きると起案が止まるだけで、サイトや記録に影響はありません。

節約したい場合は、`.github/workflows/daily.yml` の
cron を週2〜3回に変更してください。

例：月・木のみ
```
- cron: '0 20 * * 0,3'
```

---

## 取得先

| 分類 | 取得先 | 状態 |
|---|---|---|
| 国内・公的 | 総務省 新着情報 | 有効 |
| 国内・公的 | 広島県 新着情報 | **未設定** |
| 国内・AI | ITmedia AI＋ | 有効 |
| 海外・テック | TechCrunch | 有効 |
| 海外・AI | VentureBeat AI | 有効 |
| 海外・公式発表 | Google The Keyword（AI） | 有効 |

`feeds.json` で増減できます。**公式が配信しているRSSのみ**を対象とし、
スクレイピングは行いません。

取得するのは**見出し・リンク・日付だけ**です。本文は取得しません。

---

## 著作権への配慮

他社の記事を扱うため、生成の指示に次の制約を組み込んでいます。

- **本文を読んでいないことを前提に書く。**内容を要約しない、断定しない
- 「〜と題した記事が公開されている」までにとどめ、詳細は原文へ誘導する
- 見出しの引用は最小限。**見出しの羅列にしない**
- 英語の見出しも逐語訳せず、意味を自分の言葉で示す
- **記事の価値は「何が起きたか」ではなく「中小企業にとって何を意味しうるか」に置く**

これは体裁の問題ではなく、**他人の記事の代わりになる記事を作らない**
ための線引きです。

---

## 正直にお伝えしておくこと

**総務省のRSSは大半が中小企業と無関係です。**
人事、統計、電波行政などが大量に流れます。キーワードで絞っています。

一方、海外のAIメディアを加えたことで、**材料は毎日出る見込み**になりました。
むしろ多すぎる可能性があるため、取得先ごとに上限を設けています。

**フィードのURLは、この環境から接続できないため未検証です。**
手動実行の結果で、どれが通ってどれが通らないか分かります。
通らないものは `feeds.json` の `enabled` を `false` にしてください。

広島県のRSSは、フィードURLが特定できなかったため未設定です。
公式のRSS配信ページで確認して記入すれば有効になります。

---

## 動作を検証した内容

| 検証 | 結果 |
|---|---|
| RSSの解釈（RSS 1.0形式） | 4件を正しく解釈 |
| キーワードによる絞り込み | 人事・電波は除外、AI・補助金は該当 |
| 記事ページと台帳の生成 | 正常 |
| 新規記事が未承認で登録されるか | 正常 |
| 未承認のまま公開されていないか | 正しく検出 |
| 承認後に公開済みへ移るか | 正常 |
| 申請文面の生成 | 正常 |

**外部への接続だけは、この環境から検証できていません。**
実際に動くかは、手動実行で確認してください。
